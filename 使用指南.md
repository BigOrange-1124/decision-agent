# 🎯 使用指南 - 三种方式任你选

我为你提供了**三种使用方式**，从简单到完整，选择最适合你的：

---

## 🌟 方式一：浏览器界面（最简单，推荐！）

### 步骤：

1. **启动后端服务**
   ```bash
   cd D:\WorkSpace\DecisionAgent
   mvn spring-boot:run
   ```

2. **打开浏览器**
   - 访问：http://localhost:8080

3. **开始对话**
   - 在输入框输入你的问题
   - 点击"发送"按钮
   - AI会给出多维度分析

### 界面功能：
- ✨ 美观的聊天界面
- 💡 示例按钮（点击快速填入）
- 📊 实时显示AI分析结果
- 🔄 自动检测服务状态

---

## 🚀 方式二：PowerShell脚本

### 步骤：

1. **启动后端服务**
   ```bash
   mvn spring-boot:run
   ```

2. **在新窗口运行测试脚本**
   ```powershell
   .\test-api.ps1
   ```

### 优点：
- ⚡ 快速测试API
- 📋 批量测试多个场景
- 🔍 适合开发调试

---

## 🔧 方式三：Postman/cURL

### 使用 cURL：

```bash
# 启动服务
mvn spring-boot:run

# 在新终端测试
curl -X POST http://localhost:8080/api/chat `
  -H "Content-Type: application/json" `
  -d '{"message": "我要不要跳槽？我现在月薪20k，工作3年"}'
```

### 使用 Postman：

1. **创建新请求**
   - Method: `POST`
   - URL: `http://localhost:8080/api/chat`

2. **配置Headers**
   - `Content-Type: application/json`

3. **配置Body**
   ```json
   {
     "message": "我要不要跳槽？我现在月薪20k，工作3年"
   }
   ```

4. **点击Send**

### 优点：
- 🎯 适合API开发
- 📝 可以保存请求集合
- 🔍 方便查看响应详情

---

## 📊 三种方式对比

| 方式 | 难度 | 适合场景 | 界面美观度 |
|------|------|----------|-----------|
| 浏览器界面 | ⭐ 简单 | 日常使用、演示 | ⭐⭐⭐⭐⭐ |
| PowerShell脚本 | ⭐⭐ 中 | 快速测试、批量验证 | ⭐⭐ |
| Postman/cURL | ⭐⭐⭐ 复杂 | API开发、调试 | ⭐ |

---

## 🎓 推荐使用流程

### 第一次使用（推荐浏览器）：

```bash
# 1. 启动服务
mvn spring-boot:run

# 2. 打开浏览器访问
http://localhost:8080

# 3. 点击示例按钮或输入问题
```

### 开发调试（推荐PowerShell）：

```bash
# 1. 启动服务
mvn spring-boot:run

# 2. 在新窗口运行测试
.\test-api.ps1
```

### API集成（推荐Postman）：

```
使用Postman测试和保存API请求
便于后续集成到其他系统
```

---

## 💡 常见问题

### Q1: 浏览器打不开？
**A:** 确保后端服务已启动，看到 `Started DecisionAgentApplication` 日志

### Q2: 提示"服务离线"？
**A:** 检查后端是否正常运行，端口8080是否被占用

### Q3: 想要更美观的界面？
**A:** 当前界面已优化，支持实时对话、示例按钮等

### Q4: 可以移动端访问吗？
**A:** 可以！在手机浏览器输入 `http://你的电脑IP:8080`

---

## 🎯 快速开始（最简单）

```bash
# 只需两步！

# 第1步：启动服务
mvn spring-boot:run

# 第2步：打开浏览器
http://localhost:8080
```

就这么简单！开始你的AI职业规划咨询吧！ 🚀

---

## 📸 界面预览

浏览器界面包含：
- 🎨 渐变紫色主题
- 💬 聊天气泡式对话
- 🤖 AI头像 + 👤 用户头像
- 💡 4个快速示例按钮
- 📊 实时服务状态检测
- ⚡ 流畅的交互体验

**马上试试吧！** ✨

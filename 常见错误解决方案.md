# 🔧 常见错误及解决方案

## 错误1：java: command not found

### 原因
Java 未安装或未正确配置环境变量

### 解决方案

#### 步骤1：下载安装 Java 17

1. 访问 https://adoptium.net/
2. 下载 JDK 17 LTS 版本（Windows x64）
3. 运行安装程序，按默认设置安装

#### 步骤2：配置环境变量

**Windows 10/11:**

1. 右键"此电脑" → "属性"
2. 点击"高级系统设置"
3. 点击"环境变量"
4. 在"系统变量"中点击"新建"

创建以下变量：

```
变量名: JAVA_HOME
变量值: C:\Program Files\Eclipse Adoptium\jdk-17.0.x.x-hotspot
```

5. 编辑"Path"变量，添加：
```
%JAVA_HOME%\bin
```

6. 点击"确定"保存所有设置

#### 步骤3：验证安装

**关闭所有命令行窗口，重新打开**

```bash
java -version
```

应该看到：
```
java version "17.0.x"
```

---

## 错误2：mvn: command not found

### 原因
Maven 未安装或未正确配置环境变量

### 解决方案

#### 步骤1：下载安装 Maven

1. 访问 https://maven.apache.org/download.cgi
2. 下载 `apache-maven-3.9.x-bin.zip`
3. 解压到 `C:\Program Files\Maven`

#### 步骤2：配置环境变量

1. 打开"环境变量"设置
2. 在"系统变量"中点击"新建"

创建以下变量：

```
变量名: MAVEN_HOME
变量值: C:\Program Files\Maven\apache-maven-3.9.x
```

3. 编辑"Path"变量，添加：
```
%MAVEN_HOME%\bin
```

4. 点击"确定"保存

#### 步骤3：验证安装

**关闭所有命令行窗口，重新打开**

```bash
mvn -version
```

应该看到：
```
Apache Maven 3.9.x
Maven home: C:\Program Files\Maven\apache-maven-3.9.x
Java version: 17.0.x
```

---

## 错误3：端口 8080 被占用

### 原因
端口 8080 已被其他程序使用

### 解决方案

#### 方法1：修改端口

编辑 `src\main\resources\application.yml`：

```yaml
server:
  port: 8081  # 改成其他端口
```

#### 方法2：关闭占用端口的程序

1. 查找占用端口的进程：
```bash
netstat -ano | findstr ":8080"
```

2. 记下最后一列的 PID（例如：12345）

3. 结束进程：
```bash
taskkill /F /PID 12345
```

---

## 错误4：Maven 依赖下载失败

### 原因
网络问题或 Maven 仓库配置问题

### 解决方案

#### 方法1：配置国内镜像源

编辑 `C:\Program Files\Maven\apache-maven-3.9.x\conf\settings.xml`：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

#### 方法2：跳过测试编译

```bash
mvn clean install -DskipTests
```

---

## 错误5：编译错误 - 找不到符号

### 原因
代码中使用了 Lombok 但 IDE 未安装插件

### 解决方案

#### IDEA：
1. File → Settings → Plugins
2. 搜索 "Lombok"
3. 安装 "Lombok Plugin"
4. 重启 IDEA

#### Eclipse：
1. Help → Eclipse Marketplace
2. 搜索 "Lombok"
3. 安装 "Lombok Integration"
4. 重启 Eclipse

---

## 错误6：智谱AI API 调用失败

### 原因
API Key 无效或网络问题

### 解决方案

1. 检查 API Key 是否正确
2. 确保能访问 `https://open.bigmodel.cn`
3. 检查网络代理设置

---

## 错误7：Spring Boot 启动失败

### 常见错误信息

#### 7.1 "Failed to configure a DataSource"

**原因：** 项目引入了数据库依赖但未配置

**解决：** 在 `pom.xml` 中移除不需要的依赖

#### 7.2 "Could not resolve placeholder"

**原因：** 配置文件中缺少必要的配置项

**解决：** 检查 `application.yml` 中的配置是否完整

---

## 🔍 快速诊断

运行诊断脚本：

```bash
diagnose.bat
```

这会检查：
- Java 是否安装及版本
- Maven 是否安装
- 项目文件是否完整
- 端口是否被占用
- 配置文件是否正确

---

## 📞 需要帮助？

如果以上方案都无法解决问题，请：

1. **截图错误信息** - 完整截取错误堆栈
2. **运行诊断脚本** - `diagnose.bat`
3. **提供环境信息** - Java版本、Maven版本、操作系统

然后将错误信息发给我，我会帮你解决！

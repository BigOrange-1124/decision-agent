# 🚀 智谱AI版 - 启动说明

## ✅ 已完成的配置

### 1. 代码修改
- ✅ `LlmService.java` - 支持自定义 base URL（智谱AI兼容OpenAI格式）
- ✅ `application.yml` - 配置智谱AI参数
- ✅ `application-dev.yml` - 开发环境配置

### 2. 配置信息
```yaml
llm:
  api-key: c1b1c690e1974f8b84312ae41145ebd2.X7gTmeJNhpKiSSNW
  base-url: https://open.bigmodel.cn/api/paas/v4/chat/completions
  model-name: glm-4-flash
  temperature: 0.7
  max-tokens: 8000
  timeout: 1200
```

---

## 🎯 立即启动

### 方法一：直接启动（最简单）

```bash
cd D:\WorkSpace\DecisionAgent
mvn spring-boot:run
```

### 方法二：验证后启动

```bash
# 1. 验证环境配置
verify-config.bat

# 2. 启动应用
mvn spring-boot:run

# 3. 测试接口
test-api.ps1
```

---

## 🧪 测试接口

启动成功后，在新的终端窗口运行：

### PowerShell 测试
```powershell
.\test-api.ps1
```

### cURL 测试
```bash
curl -X POST http://localhost:8080/api/chat `
  -H "Content-Type: application/json" `
  -d '{"message": "我要不要跳槽？我现在月薪20k，工作3年"}'
```

### 浏览器测试
访问: http://localhost:8080

---

## 📊 预期日志

启动成功后，应该看到：

```
LLM Service initialized with model: glm-4-flash, provider: https://open.bigmodel.cn/api/paas/v4/chat/completions
Started DecisionAgentApplication in X.XXX seconds
```

---

## 🔧 配置说明

### 智谱AI vs OpenAI

| 配置项 | 智谱AI | OpenAI |
|--------|--------|--------|
| API Key | 以 `.` 结尾的长字符串 | 以 `sk-` 开头 |
| Base URL | `https://open.bigmodel.cn/api/paas/v4/chat/completions` | 留空 |
| Model Name | `glm-4-flash`, `glm-4-plus` | `gpt-4`, `gpt-4o-mini` |

### 切换模型

编辑 `application.yml`：

```yaml
llm:
  model-name: glm-4-plus  # 更强大的模型
  # model-name: glm-4-flash  # 更快的模型
```

---

## 📖 相关文档

- `README.md` - 完整项目文档
- `QUICKSTART.md` - 快速启动指南
- `ZHIPUAI_SETUP.md` - 智谱AI详细配置
- `PROJECT_STRUCTURE.md` - 项目结构说明

---

## ❓ 常见问题

### Q: 启动失败，提示连接超时
**A:** 检查网络连接，确保能访问 `open.bigmodel.cn`

### Q: 提示 API Key 无效
**A:** 访问 https://open.bigmodel.cn/ 获取新的 API Key

### Q: 想换回 OpenAI
**A:** 修改 `application.yml`，将 `base-url` 留空，`api-key` 改为 OpenAI 的 key

---

## ✨ 下一步

1. ✅ 运行 `mvn spring-boot:run` 启动应用
2. ✅ 运行 `.\test-api.ps1` 测试接口
3. 📖 查看 `README.md` 了解更多功能
4. 🔧 参考 `ZHIPUAI_SETUP.md` 自定义配置

---

**祝你使用愉快！** 🎉
